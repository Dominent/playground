{(import TItem from './TItem.jshtml';)}

<div class="row">
    {{
        let output = [];
        for (let item of items){
            output = TItem(output)
        }

        return output;
    }}
</div>